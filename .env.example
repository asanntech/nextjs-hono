# フロントの API 接続先。
# ローカル開発: 空にして同一オリジン（Next.js にマウントした /api/*）を使う。
# 本番: 別サーバーの URL を指定（例: https://api.example.com）。
NEXT_PUBLIC_API_URL=

# standalone Hono サーバーの待ち受けポート（src/server/index.ts）。
PORT=8787

# standalone サーバーが許可する CORS オリジン（本番でフロントと別ホストの場合に設定）。
CORS_ORIGIN=*
